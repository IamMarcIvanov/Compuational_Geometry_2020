#include "primitives.hpp"

/** Indicates if the turn is PQR is right.
*/
bool right_turn(Point p, Point q, Point r);

/** Finds the convex hull using Andrew's Algorithm.
*/
void AndrewAlgorithm(ConvexHull ch);
